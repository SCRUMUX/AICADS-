## API Report File for "@ai-ds/core"

> This is a placeholder. Generate the real snapshot with:
>
> ```bash
> npm install --save-dev @microsoft/api-extractor typescript
> npm run api:extract
> ```
>
> The generated report lives at `etc/ai-ds-core.api.md` and must be committed.
> CI runs `npm run api:check` and fails if the on-disk report drifts from the
> current `src/index.ts` surface.

```ts
// (placeholder — populated by api-extractor on first run)
```
